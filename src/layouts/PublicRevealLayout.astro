---
import BaseLayout from "./BaseLayout.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<BaseLayout title={`${title} | Secret Sánchez`}>
  <main>
    <div class="page-shell">
      <slot />
    </div>
  </main>
</BaseLayout>
